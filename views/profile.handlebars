<div>
        <nav style="position: relative; bottom: 0">
        <!-- Conditionally render login or logout links -->
        {{#if logged_in}}
        <button class="logout-btn" id="logout">Logout</button>
        {{else}}
        <a href="/login">login</a>
        {{/if}}
      </nav>
</div>
<article>
<div class="user-info">
    <h3 class="user-font">Welcome, {{name}}!</h3>
    <div class="user-text">
    <p>Username: {{username}}</p>
    <p>Grade: {{grade}}</p>
    <p>Favorite Animal: {{animal}}</p>
    <p>Favorite Color: {{color}}</p>
    <p>Favorite Food: {{food}}</p>
  </div>
</div>
</article>
    <div class='topics'>
    <div id='topic1'>
      <a href="/art">
        <h2>The Arts</h2>
        <img src='/images/artGopher.png' id='art' />
      </a>
    </div>
    <div id='topic2'>
      <a href="/geography">
        <h2>Geography</h2>
        <img src='/images/geographyGopher.png' id='geography' />
      </a>
    </div>
      <div id='topic3'>
        <a href="/technology">
          <h2>Technology</h2>
          <img src='/images/techGopher.png' id='tech' />
        </a>
      </div>
    </div>
  </div>
</div>

{{!-- this page is displayed directly after login. TODO: front end: add in user data & make cute, add button to navigate to chatroom selection - create & connect to corresponding js file via script tag - back end: add routes file in controllers--}}