<div class="">
    {{#each this}}
    <section class="">
        <div class="">
            {{user.username}}
        </div>
        <div class="text">
            {{art_comment_text}}
        </div>
    </section>
    {{/each}}
</div>