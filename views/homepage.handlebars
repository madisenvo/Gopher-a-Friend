{{!-- this page is displayed when a user selects logout TODO: front end: all formatting--}}
<div>
  <h2>Login with your koala-fications to get chatting!</h2>
      <nav style="position: relative; bottom: 0">
        <!-- Conditionally render login or logout links -->
        {{#if logged_in}}
        <button class="loginBtn"href="/profile">Profile</button> |
        <button class="logout-btn" id="logout">Logout</button>
        {{else}}
        <a class="loginBtn" href="/login">Login</a>
        {{/if}}
      </nav>

  <img src="/images/loginBackgroundImg.png" alt="gopher" width="500" height="500" class="center">
</div>